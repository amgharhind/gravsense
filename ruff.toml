[lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",   # line too long — not enforced
    "E741",   # ambiguous variable name (l, O, I)
    "W291",   # trailing whitespace
    "I001",   # import sorting — handled by developer tooling, not CI gate
]
